# frontend-build

Build and verify the Next.js frontend:
1. `cd web && npm install`
2. `cd web && npm run build` — must pass zero errors
3. `cd web && npm run lint` — zero ESLint errors
4. Verify no `any` types introduced
5. Check bundle size report if available

Usage: `/frontend-build`
