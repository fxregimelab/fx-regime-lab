# Frontend architecture

**There is no shipped frontend in this repository.** The former Next.js App Router tree under `web/` was removed.

## Where to look next

- **UX and layout intent:** `claude-design/` (prototype JSX, read-only reference).
- **Supabase reads the old app used:** [[DATA_READS_SPEC]] (inventory of `queries.ts` before deletion).
- **Hosting / Worker:** [[HOSTING_AFTER_UI_REMOVAL]].

When you add a new app, replace this document with the actual route map, data-fetching strategy, and component layout for that stack.
