# TASK.md — Current Sprint

**Status:** Shipped web UI removed (no `web/`). Next UI work starts from `claude-design/` and `docs/DATA_READS_SPEC.md`.

## Active Tasks

- [ ] Define new frontend stack and scaffold app when ready

## Notes

- Hosting / secrets cleanup: `docs/HOSTING_AFTER_UI_REMOVAL.md`
- When a TypeScript app exists again: `supabase gen types typescript --local > <app>/lib/supabase/database.types.ts` (never edit generated types by hand)
