# Profile image

Replace **`shreyash_sakhare.png`** in this folder with the final portrait when ready.

- **Canonical path:** `/images/shreyash_sakhare.png` (used by `AboutStrip` and `/about`).
- **Suggested dimensions:** ~280×320 px (matches `next/image` width/height in those components).
- **Format:** PNG or WebP; keep the filename unless you update imports in `AboutStrip` and `about/page.tsx`.
